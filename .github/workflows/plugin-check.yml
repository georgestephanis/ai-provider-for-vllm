name: Plugin Check

on:
  workflow_run:
    workflows: [ 'Build Plugin Zip' ]
    types:
      - completed

# Disable permissions for all available scopes by default.
# Any needed permissions should be configured at the job level.
permissions: {}

jobs:
  plugin-check:
    name: Run Plugin Check
    runs-on: 'ubuntu-latest'
    # Only run if the build workflow succeeded and was triggered by a pull_request
    if: >
      github.event.workflow_run.event == 'pull_request' &&
      github.event.workflow_run.conclusion == 'success'
    permissions:
      contents: read
      actions: read
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          show-progress: ${{ runner.debug == '1' && 'true' || 'false' }}
          persist-credentials: false

      - name: Download built zip
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          path: ${{ github.event.repository.name }}
          pattern: ai-provider-for-vllm-plugin-zip-pr*
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Unzip built zip
        run: unzip ${{ github.event.repository.name }}/ai-provider-for-vllm.zip -d ${{ github.event.repository.name }}

      - name: Display structure of downloaded files
        run: ls -R
        working-directory: ${{ github.event.repository.name }}/${{ github.event.repository.name }}

      - name: Run plugin check
        uses: wordpress/plugin-check-action@6f5a57e173c065a394b78688f75df543e4011902 # v1.1.5
        with:
          build-dir: ${{ github.event.repository.name }}/${{ github.event.repository.name }}
          ignore-warnings: true
          exclude-directories: |
            build
            vendor
